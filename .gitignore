node_modules
.env
.openzeppelin

#Hardhat files
cache
artifacts

# Editors
.idea/**

# Signed Safe bundles are bearer authorizations: anyone holding a bundle with
# `threshold` signatures can execute it. Never commit these.
safe-bundles/
